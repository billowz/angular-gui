angular.module('ngui.panel', ['ngui.utils', 'ngui.collapse'])
.directive('nguiPanel', ['utils', function ($transition, utils) {
  return {
    link: function (scope, element, attrs) {

      
    }
  };
}]);